#include <iostream>
#include <sstream>
#include <string>
using namespace std;

class CustomMatch {
private:

public:
  CustomMatch();
  void printCustomMatchMenu();
  void generateCustomMatch(int userInput);
};
